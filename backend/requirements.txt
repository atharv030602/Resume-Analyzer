# --- Core API runtime (always required) ---
# Ranges, not hard pins, so this co-resolves cleanly with the optional
# AI stack (requirements-ai.txt), which moves fast.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
python-multipart>=0.0.9,<1
python-dotenv>=1,<2

# --- Document parsing ---
pypdf>=4.3,<6
python-docx>=1.1,<2

# --- Persistence (optional at runtime; DB_ENABLED=false to skip) ---
sqlalchemy>=2,<3
pymysql>=1.1,<2
